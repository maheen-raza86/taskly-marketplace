<?php

namespace App\Http;

use Illuminate\Foundation\Http\Kernel as HttpKernel;

/**
 * In Laravel 11, this file is intentionally minimal.
 * Middleware registration is handled in bootstrap/app.php.
 * Route middleware aliases (like 'role') are set there via ->withMiddleware().
 */
class Kernel extends HttpKernel
{
    //
}
